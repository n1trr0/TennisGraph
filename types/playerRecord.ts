export interface PlayerRecord {
    player_id: string;
    total_wins: number;              // Total de victorias
    total_losses: number;            // Total de derrotas
    grand_slam_wins: number;         // Victorias en Grand Slams
    grand_slam_losses: number;       // Derrotas en Grand Slams
    finals_wins: number;             // Victorias en finales
    finals_losses: number;           // Derrotas en finales
}
